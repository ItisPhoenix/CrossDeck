package com.crossdeck.client.ui.theme

/**
 * Motion scale mirroring windows-host's Colors.xaml Motion.* tokens — keep both in sync.
 * Small = button press / popup open. Large = page swipe / sheet open.
 */
object Motion {
    const val DURATION_SMALL_MS = 150
    const val DURATION_LARGE_MS = 250
}
