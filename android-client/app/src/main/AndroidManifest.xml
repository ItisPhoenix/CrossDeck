<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to open the WebSocket connection to the Windows Host on the LAN -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required for NSD (mDNS) discovery, wired up in Milestone 2 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Required for QR Code camera scanner -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:label="CrossDeck Client"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrossDeckClient"
        android:usesCleartextTraffic="true">
        <!--
          No android:icon set yet — generate real launcher icons via Android Studio's
          Image Asset Studio (right-click res/ > New > Image Asset) before shipping.
          Omitting it is fine for local dev/testing; Android uses a default icon.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CrossDeckClient">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QrScannerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CrossDeckClient" />
    </application>

</manifest>
