# Comment rule

Max 1 line per comment. Ever. No exceptions for "important" fixes, root-cause bugs, debug code, new functions.

No comment > 1 line, even if non-obvious. Explain in chat, not code.

Before every Edit/Write that adds a `//` or `/** */`: check line count first. If 2+ lines, cut it now, don't ship it.
